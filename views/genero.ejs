<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h1>Genero: <%= genero %></h1>
  <ul>
    <% bandas.forEach(element => { %>
     <li>
      <a href="http://localhost:3000/<%= element.id %>">
        <p><%= element.nombre %></p>
        <img src="/<%= element.cover %>" style="width: 200px" alt="">
      </a>
    </li>
    <% }) %>
  </ul>
</body>
</html>